name: Deploy Frontend (Vite)

on:
  push:
    branches:
      - server
      # change to master if your repo uses master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy to Server
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }} << 'EOF'
            # -------------------------------
            # Deploy frontend
            # -------------------------------
            cd /var/www/html/react-Vibe/vibeconnect-frontend || exit 1

            echo "🚀 Pulling latest code"
            git fetch origin
            git reset --hard origin/server

            echo "📦 Installing dependencies"
            npm install || exit 1

            echo "🧠 Increasing Node memory"
            export NODE_OPTIONS=--max-old-space-size=8192

            echo "🏗️ Building frontend"
            npm run build || exit 1

            echo "✅ VibeConnect deployed successfully"
          EOF
